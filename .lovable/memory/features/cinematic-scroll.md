Cinematic overlay scroll: hero background rendered via createPortal to document.body, fully independent from page scroll flow. Hero is fixed (z-0, isolate), crystal ball + zodiac + menu items in fixed layer (z-[12]) with paddingTop 5vh and desktop marginTop offsets. Scrollable content is z-10 in a separate section. Crystal ball layer at z-[12] so menu tabs on sides of ball are clickable above scroll layer. DailyRitualSection uses semi-transparent glassmorphism. Hero figure overlays are light (background/30-40) to keep character visible. Header is fully transparent, no blur/gradient. App.css #root has no max-width or padding constraint.
