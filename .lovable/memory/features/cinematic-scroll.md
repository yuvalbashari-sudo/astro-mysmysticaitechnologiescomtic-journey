Cinematic overlay scroll: hero rendered via createPortal to document.body as a completely independent fixed layer (z-0, isolate). Crystal ball + zodiac + menu items + CTA all live inside this portal. Scrolling page content is a separate div (z-10) in Index.tsx with a full-screen spacer div (h-screen) pushing content below the hero viewport. DailyRitualSection uses semi-transparent glassmorphism. Hero figure overlays are light (background/30-40). Header is fully transparent, no blur/gradient. App.css #root has no max-width or padding constraint. HeroSection manages its own modal state internally.
